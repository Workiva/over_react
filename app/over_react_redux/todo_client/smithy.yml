project: dart
language: dart

# Dart 1.23.0 image from https://github.com/Workiva/smithy-runner-generator
runner_image: drydock-prod.workiva.net/workiva/smithy-runner-generator:153818

before_script:
  - dart --version
  - pub get --packages-dir
  - pub run over_react_format:bootstrap --check
  - pub run dart_dev format --check
  - pub run dart_dev analyze

script:
  - DART_FLAGS=--checked xvfb-run -s '-screen 0 1024x768x24' pub run dart_dev test

after_script:

artifacts:
